[tool.ruff]
line-length = 100
target-version = "py310"
extend-exclude = [
    "venv",
    "build",
    "dist",
    "data",
    "logs",
]

[tool.ruff.lint]
select = ["E", "F", "I", "B"]
ignore = ["E501"]

[tool.ruff.format]
quote-style = "double"
line-ending = "native"
